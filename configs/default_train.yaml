seed: 42
train:
  max_epochs: 1
  max_steps: 10
  log_interval: 10
  save_interval: 100
  output_dir: outputs
  gradient_clip_norm: null
  distributed: false
  num_workers: 2
  amp: false
optimizer:
  name: adam
  lr: 0.001
